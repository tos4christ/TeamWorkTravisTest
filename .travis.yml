language: node_js
node_js:
- "10"

services:
  - postgresql

addons:
  postgresql: "9.5"

before_script:
  - npm install
  - psql -c 'create database teamwork;' -U postgres
  - npm install -g jasmine
  - npm run test-init
 

script:
  - npm run buildtables
  
  

after_success:
  - npm run coverage
  - npm run coveralls
